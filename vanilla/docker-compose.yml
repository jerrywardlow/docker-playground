---

web:
    build: .
    image: vanilla
    ports:
        - "6680:80"
    depends_on:
        - db:database

db:
    image: mysql:5.7
    ports:
        - "3306:3306"
    environment:
        MYSQL_ROOT_PASSWORD: vanillapassword
        MYSQL_DATABASE: vanilladb
